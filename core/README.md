# Core module

This directory will contain the platform-neutral resonance engine.

The first implementation should expose a small API for target selection, carrier updates, precision, stable-hold detection, and resonance events. No Android Compose or browser DOM code belongs here.

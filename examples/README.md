# Examples

Host examples will demonstrate how an application connects a TUNE control, HUD, audio layer, and visual field to the core resonance engine.

The first example will model the six-element REZONATOR seed set.
